package:
  name: action-multiple-variants-fixture
  version: "0.1.0"

build:
  number: 0
  script:
    - mkdir -p "$SP_DIR"
    - echo '__version__ = "0.1.0"' > "$SP_DIR/action_multiple_variants_fixture.py"

requirements:
  host:
    - python
  run:
    - python

test:
  imports:
    - action_multiple_variants_fixture
